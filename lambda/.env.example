# Example environment variables (replace with actual values)
AWS_RDS_HOST=your-rds-hostname.region.rds.amazonaws.com
AWS_RDS_PORT=5432
AWS_RDS_USER=your-admin-username
AWS_RDS_PASSWORD=your-admin-password
DATABASE_URL=postgresql://user:password@hostname:port/database

# AWS region
AWS_REGION=us-east-1

# Stage
STAGE=dev

# Lambda ARNs (these will be populated after first deployment)
# FORECAST_SYNC_LAMBDA=arn:aws:lambda:region:account-id:function:data-dock-sync-dev-forecastSync
# INTECT_SYNC_LAMBDA=arn:aws:lambda:region:account-id:function:data-dock-sync-dev-intectSync 